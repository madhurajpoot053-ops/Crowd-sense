version: '3.9'

services:
  crowd-sense:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - SECRET_KEY=change_me_in_production
      - FLASK_ENV=production
      - PORT=5000
    restart: unless-stopped
